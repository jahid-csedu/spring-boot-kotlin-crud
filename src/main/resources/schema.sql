CREATE TABLE `restpoint`.`employee`
(
    `id`         INT          NOT NULL,
    `name`       VARCHAR(120) NOT NULL,
    `age`        INT NULL,
    `department` VARCHAR(200) NOT NULL,
    PRIMARY KEY (`id`)
)